=form_for @assignee, url: assignee_path, action: "PUT" do |f|
  =render "form", f: f

  = f.submit "Update"
  %br
  %br
  = button_to 'Delete this Item', assignee_path, method: "delete", data: { confirm: "Are you sure?" }, class: "btn btn-danger form-control"