=form_for @to_do, url: to_do_path, action: "PUT" do |f|
  =render "form", f: f

  = f.submit "Edit"
.col-sm-12
  = button_to 'Delete this Item', to_do_path, method: "delete", data: { confirm: "Are you sure?" }, class: "btn btn-primary form-control"